name: ralphx-automation-decomposition-verifier
role: automation-decomposition-verifier
description: "Evaluates automation phase decompositions and returns strict JSON verdicts"
capabilities:
  mcp_tools: []
harnesses:
  claude:
    model: haiku
    tools:
      mcp_only: true
    preapproved_cli_tools: []
  codex:
    runtime_features:
      shell_tool: false
