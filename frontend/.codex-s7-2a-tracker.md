# Batch A task-lifecycle gate tracker

## Scope
- Owned: `src/lib/remote/agent-gate.ts`, `src/components/remote/agent-gate-guard-manifest.ts`, wired task UI components, and their tests.
- Excluded: `src-tauri/**`, `src/api/tasks.ts` behavior, unrelated worktree diffs.

## Done
- Read required project/frontend/icon-button instructions and RepositorySettingsSection exemplar.
- Verified all 11 ops are registered `agentControl` rows in `docs/generated/remote-commands.json`.
- Verified none of the 11 currently appears in `AGENT_GATED_AFFORDANCES` or the inert exemptions.
- Traced archive/restore/pause/block through `useTaskMutation`; group pause/cancel through the same hook; reorder through `useStepMutations`.
- Added six live affordance rows and wired shared task/group menus, running-process pause, and Agents overlay archive/restore.
- Added behavioral gate tests using the real environment store.
- `bun run typecheck` passed, including all drift/mirror/census guards and `tsc --noEmit`.

## Pending
- External/orchestrator verification: Prettier and Vitest remain blocked locally; typecheck passed.

## Decisions
- `create_task` remains out of scope and inert as directed.
- No production UI consumer exists for `inject_task`, `start_step`, `complete_step`, `fail_step`, or `reorder_task_steps`; classified as host-driven/no-surface and intentionally left without dead affordance rows.
- Gate at rendered controls/handlers, not in `src/api/tasks.ts` or the shared mutation hook.
