# S7.1 unknown-outcome reconciliation

## Scope

- Modify only `src/lib/queryClient.ts` and `src/lib/queryClient.test.ts`.
- Add global unknown-outcome invalidation to each environment-scoped QueryClient.

## Done

- Read root and frontend CLAUDE instructions plus stateful-workflow review rules.
- Confirmed installed TanStack Query 5.100 supports `Register.mutationMeta` augmentation and `MutationCache.onError` receives the mutation.
- Identified `createQueryClient()` as the owning per-environment seam.
- Added behavioral regression tests for broad/narrow invalidation, both non-matching error paths, no resend, and environment isolation.
- Attempted the TDD red run; Vitest startup was blocked by the documented symlinked `node_modules/.vite-temp` EPERM condition.
- Added a per-client `MutationCache.onError` that uses the canonical transport predicate and awaits environment-local invalidation.
- Typecheck passed, including repository drift checks and `tsc --noEmit`.
- Reviewed the final diff: defaults are byte-for-byte preserved; no retry/resend/timer/message path exists; non-unknown errors return before effects; only the closed-over client is reachable.
- `git diff --check` passed for all scoped files.

## Pending

- Orchestrator rerun: Prettier and Vitest are sandbox-blocked locally.

## Decisions

- Type mutation metadata through TanStack Query's idiomatic `Register.mutationMeta` augmentation; retain an index signature so existing unrelated metadata remains valid.
- Close over the newly constructed client so invalidation is structurally environment-local and never reads ambient environment state.
- Treat an explicitly declared empty key list as a deliberate narrow-to-none declaration; only absent metadata selects broad invalidation.
- Await invalidation in the cache callback so mutation settlement and tests observe completed reconciliation; do not add retry or message behavior.
- Gate status: Prettier BLOCKED (`bunx` tempdir PermissionDenied); Vitest BLOCKED (symlinked `.vite-temp` EPERM); typecheck PASS (exit 0).
