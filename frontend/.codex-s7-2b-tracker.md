# Batch B — personas + ideation affordance gates

## Scope

- Gate only persona/ideation controls for the 15 requested operations.
- Allowed infrastructure: `src/lib/remote/agent-gate.ts` and `src/components/remote/agent-gate-guard-manifest.ts`.
- No API behavior changes and no backend changes.

## Done

- Read repository/frontend instructions and the two required gate exemplars.
- Confirmed all 15 operation strings occur in `docs/generated/remote-commands.json`.
- Started API-to-control consumer tracing.
- Wired all eight persona operations across the editor, shared management row, and artifact panel.
- Added focused row and artifact-panel gate tests.

## Pending

- Add editor gate coverage.
- Resolve the ideation settings soft-disable conflict without changing the out-of-scope shared toggle primitive.
- Wire both user-controlled ideation-title editing surfaces.
- Re-run per-file formatting and the full Vitest suite outside the sandbox.

## Decisions

- Gate at the component owning the interactive control; do not alter `usePersonas.ts` or ideation API behavior.
- Skip `archive_task_proposal`, `reorder_proposals`, `assess_proposal_priority`, `assess_all_priorities`, and `remove_proposal_dependency`: no production component consumes their API/hook surface.
- `update_ideation_settings` has real controls, but their shared `ToggleSettingRow` primitive exposes only a true `disabled` prop; the requested soft-disable contract cannot be expressed from the allowed ideation component alone.
- `update_ideation_session_title` has user controls in `AppTopBar`, `AgentsChatHeader`, and `AgentsSidebar`; left pending rather than adding an unwired affordance row.
- Sandbox gates: `bunx prettier` is blocked by bun tempdir permissions; Vitest is blocked writing the symlinked `node_modules/.vite-temp` cache. Typecheck passes.
