# Wave B5b Tracker

## Scope
- Add spawn-free remote mute and persona twins without changing local command behavior.
- Add frontend unarchive affordance gating and repoint mute/persona remote API branches.
- Update remote registry, capability ledger, generated manifest/mirror/census, and focused tests.

## Constraints
- TDD-first; no production inline test modules.
- Local command paths remain byte-identical in behavior.
- Remote persona flags are host-side globals; running agents are rejected without stop/rollback.
- No `startup_background.rs` edits; scoped formatting only; do not run Vitest.
- Rust tests require a final repository-mandated `cargo clean` despite the request's conflicting no-clean constraint.

## Findings / Decisions
- Commands-resident B3a twins call hidden `*_for_state` helpers and are explicitly registered in the remote ledger.
- Mute twin shares the local body but selects the recovery-free workspace projection; local selects recovery-enabled projection.
- Persona twin reads host globals in its public helper, uses Reject policy with pre/post-binding running checks, and never receives a chat-service stop seam.
- Test-only explicit flags avoid mutating process-global persona state; an initial override-based test proved racy against the broad `persona` filter and was removed.
- Existing running rejection copy is `Cannot change persona while the agent is running`; frontend propagates that host error unchanged.

## Progress
- [x] Read root/backend/frontend instructions and remote facade/API/test rules.
- [x] Add focused backend/frontend tests (frontend tests intentionally not run).
- [x] Implement backend twins and wiring.
- [x] Implement frontend gates/API repoints.
- [x] Regenerate remote manifest, capability mirror, and census (593 ledger rows).
- [x] Run requested gates and capture raw tails; frontend Vitest intentionally not run.
- [x] Review diff and false-success/TOCTOU behavior.

## Final Review
- Remote mute reaches the recovery-free builder with a seeded workspace and persists the same fingerprint as local; no scheduler/chat-service carrier is accepted by the twin.
- Reject policy checks running state before the binding and immediately after it; a post-write race returns the named error without rollback or provider-session reset, as documented.
- Running rejection leaves binding absent; idle switch writes binding and conditionally clears provider session; disabled personas fail before reads/writes.
- Unarchive resolves through the already-registered restore op and the action callback fails closed on the gate.
- Local mute/persona API command names and local stop behavior remain unchanged.
- Repository-mandated post-test `cargo clean` completed (18.6 GiB removed), overriding the request's conflicting no-clean constraint.
