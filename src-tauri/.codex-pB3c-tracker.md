# Wave B3c tracker

## Scope
- Repoint queued-message gates to the B3a/B3b remote twins.
- Add remote list/cancel/send-now API paths while preserving local branches.
- Surface remote hydration failure and reconcile host-restored queue order.
- Update focused frontend tests; do not run Vitest/Playwright.

## Contracts found
- Remote queue identity: for project conversations, backend queue context id is the conversation id.
- Twin inputs are flat Tauri params with camelCase JS keys (`conversationId`, `messageId`, `queuedMessageId`, `expectedActiveRunId`, `requestId`).
- Queue-list payload uses the existing snake_case `QueuedMessageResponse` wire schema.
- Send intent statuses are `pending | starting | completed | failed | failedStale`; benign/rehydration detail lives in nullable `errorCode` and `result`.

## Progress
- [x] Read required project/rule docs and owning precedents.
- [x] Write/update focused tests (not run per worktree constraint).
- [x] Implement gate, hydration, API, action, and component changes.
- [x] Run final permitted typecheck/drift/census gates and review diff.

## Validation
- `cd frontend && bun run typecheck` — PASS (includes drift/census preflight; `tsc --noEmit`).
- `node scripts/check-remote-transport-drift.mjs .` — PASS, 597 literal names, 0 dynamic/bypasses/unclassified.
- `node scripts/generate-remote-coverage-census.mjs --check .` — PASS after one authorized regeneration.
- Vitest/Playwright intentionally not run per Wave B3c constraint.
