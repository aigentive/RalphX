# Wave C2 tracker

## Scope
- Cache inspected repository capability without changing local host projections.
- Remote projection exposes only kind, has_remote, inspected_at, and optional failure message.
- Fetch/push URL exposure is deferred pending owner decision.
- Parallel-agent-owned MCP policy files are excluded.

## Progress
- [x] Read required root/backend/remote-facade rules and relevant migration/stateful/frontend rules.
- [x] Add migration/repository/write-through/remote projection tests.
- [x] Add entity, repositories, migration, and AppState wiring.
- [x] Persist existing inspections and project the non-stale cached snapshot remotely.
- [x] Adapt frontend project schema/projection without rendering remote URLs.
- [x] Update ledger reason and regenerate/check remote metadata.
- [x] Run requested gates and record raw tails.

## Validation
- Layering, warning-free cargo check, remote_workspace tests, manifest regeneration, mirror/census/drift, frontend typecheck, migration validation, and scoped rustfmt passed.
- `project` filter: C2 tests passed; 633 passed / 2 unrelated sandbox failures creating worktrees outside the writable checkout.
- `capability` filter: C2 tests passed; 127 passed / 2 unrelated pre-existing failures (global runtime probe cache assertion and included git rewrite expectation), 24 ignored.

## Decisions
- Local project responses remain sourced exclusively from live inspection; cache writes are side effects only.
- A missing row or working-directory mismatch projects as `null` / frontend `notInspected`.
- Cache read errors remain errors; only actual absence/staleness becomes `null`.

## Parallel ownership
- Do not touch `commands/mcp_policy_commands.rs`, `commands/remote_mcp_policy_commands.rs`, the `mcp_catalog_snapshot` migration, `frontend/src/api/mcp-policy.ts`, or `McpSettingsSection.tsx`.
