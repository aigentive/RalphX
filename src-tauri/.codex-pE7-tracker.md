# Wave E7 tracker

## Goal
- Render the existing publish/read surface for paired remote clients while gating only publish and close-PR by manifest absence.
- Preserve local behavior and reuse C4 host-snapshot/null-copy presentation.

## Constraints
- Frontend-only production/test edits; this tracker is the sole allowed `src-tauri` file.
- TDD with real remote envelopes and explicit-null serialized payloads.
- No publish/close intent twins, generated files, dependency changes, commits, or pushes.

## Investigation
- Blanket remote return is in `AgentsArtifactPane.tsx` before `AgentPublishPanel` mounts.
- `AgentsPublishPanel` already owns C4 change snapshot labelling; diff components own their corresponding host-snapshot/null-copy presentation.

## Progress
- [x] Read required project and remote-facade instructions.
- [x] Map publish sub-panels, workspace read hydration, actions, and gate rows.
- [x] Add remote publish-surface regression tests.
- [x] Implement minimal gate/read wiring.
- [x] Run full Vitest, typecheck, and remote drift guard (Vitest blocked before collection by sandbox EPERM writing Vite config cache through the external `node_modules` symlink).

## Findings / Decisions
- `AgentPublishPanel` had live publish and close-PR assumptions because the parent previously prevented remote mounting; both now consume manifest-derived gates.
- Existing C4 review/change snapshots already own `(host snapshot)` freshness and null-capture copy; reused unchanged.
- Remote null snapshots suppress the generic empty-diff body so the C4 not-yet-captured copy is the sole state; local null behavior remains unchanged.
- Other sub-panels expose partial-read states (for example checks/annotations) through their existing unavailable vocabulary; no fabricated fallback was added.
