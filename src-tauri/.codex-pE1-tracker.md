# Wave E slice 1 tracker

## Scope
- Frontend-only gate alignment, guard coverage, and permission hydration fixes.
- Generated capability manifest is read-only; tests are written but not run.

## Progress
- [x] Read required project and remote-facade instructions.
- [x] Fix stale resume/restart affordance mappings and regression tests.
- [x] Gate execution-plan controls on the invoked plan operations.
- [x] Add all missing gate-wired production files and resolve mismatches.
- [x] Route permission pending reads through the registered remote-aware API.
- [x] Run typecheck and the two remote drift/census checks.

## Findings
- `taskResume`, `taskRestart`, and `executionResume` still map to obsolete local command names.
- Agents plan controls gate generic execution operations while invoking distinct `*_execution_plan` operations.
- `permissionApi.listPendingPermissionGates` is the established registered remote-aware read seam.
- Group resume is a distinct dispatch (`request_remote_group_resume`), so it has its own affordance row and indirection entry.
- Op-consistency mismatches fixed: task resume, task restart, group resume, execution resume, and the plan pause/resume/stop controls now name their exact dispatch operations.
- All 18 missing hook consumers are listed; existing FinalizeConfirmationDialog and AgentsStartComposer indirections remain legitimate.
- Strict hydration failure now replays buffered permission events instead of discarding them.

## Validation
- `bun run typecheck`: passed (including configured remote drift/census prechecks).
- `node scripts/check-remote-transport-drift.mjs .`: passed.
- `node scripts/generate-remote-coverage-census.mjs --check .`: passed.
- Vitest/Playwright intentionally not run per request.
