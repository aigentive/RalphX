# Wave B2a tracker

## Scope
- Backend-only remote plan-approval intent twin, following the remote execution-resume precedent.
- Preserve exact local HTTP approval behavior through a shared AppState seam.
- Add domain/repository/migration, command/poll, dispatcher/stale recovery, registry/ledger/census wiring, generated artifacts, and focused behavioral tests.

## Done
- Created resumable tracker.
- Fix round 2: ran `python3 scripts/check-layering.py` first and captured 16 `root_application_no_commands_or_http_imports` violations (13 startup dispatcher, 3 startup test imports); baseline was not updated.
- Fix round 2: mapped the offending B1/B2a seams and confirmed the owning application precedents (`task_restart`, `plan_artifact_approval`, startup application services).
- Fix round 2: mechanically re-homed active/execution state, remote resume/plan intent DTOs/constants/seams, execution recovery/control/status/running support, the four host execution seams, task command DTOs, and the plan-approval host seam into `application`; command/HTTP surfaces now delegate and re-export for compatibility.
- Fix round 2: layering ratchet passes with 279 tracked entries, both hard-zero rules clean, no baseline update, and `git diff -- scripts/` empty.
- Fix round 2 gates pass: `cargo check --tests --features test-utils`; `remote_plan` 8/8; `remote_resume` 18/18; `startup_remote` 9/9; `plan_artifact_approval` 13/13; scoped rustfmt completed for every touched leaf Rust file.
- Read the required project/backend/remote/stateful/test rules and traced the B1 resume precedent across command, repositories, migration, AppState, dispatcher, startup, registry, ledger, and authority census.
- Extracted `approve_plan_artifact_for_state`; the HTTP handler delegates to it and the host actor remains `PlanApprovalActor::User` by wire-field absence.
- Added the remote plan-approval entity/status, repository trait, memory/SQLite implementations, forward-only migration, and all five extant AppState constructor sites.
- Added spawn-free request/poll commands with active-planning validation, artifact validation, exact-triple dedup, conflict refusal, persist-last behavior, and fail-closed poll reads.
- Added CAS dispatcher integration to the existing remote-resume loop, 300-second startup stale settlement, host revalidation, plan-changed error classification, and terminal persistence.
- Added command registry, remote allowlist, SeedsSpawnTriggeringState ledger reason (including tasks-enabled Codex assessor fan-out), authority surface, detector-B census 576 -> 578, and regenerated outputs.
- Added focused request/poll/migration behavioral tests; exact-triple dedup, persist-last, inactive/missing authority, poll missing, schema/index/round-trip/idempotence are green.
- Re-ran the existing HTTP planning-flow create/approve integration test through the extracted handler seam; it passed.
- Ran requested gates. Compile, remote-plan, approval, manifest regeneration, mirror/census generation, and transport drift passed. The remote-server suite's non-socket pins passed; 32 socket-binding tests were environment-blocked with `Operation not permitted` (383 passed, 26 ignored).
- Ran the false-success review: claim/read errors propagate without terminalization; only `Starting` rows can settle; completed/failed/stale rows cannot be reclaimed; side effects occur only inside the shared approval authority transaction and terminal result follows it.
- Added `startup_remote_plan_tests.rs`, registered beside the B1 dispatcher suite, with production-dispatch coverage for success/re-entry, changed-plan rejection, archived authority at claim time, and stale-lease no-retry; all rejection paths assert no approval row and success proves `approved_by = user` plus unchanged re-dispatch state.
- Closed the successful-host-action serialization gap for all three remote dispatcher families: plan approval and B1 execution resume can no longer strand `Starting`, and task actions no longer misclassify a successful host effect as failed; serialization now warns and completes with JSON `null`.
- Re-ran the requested fix-round gates: `cargo check --tests --features test-utils` passed; `remote_plan` passed 8/8; combined `startup_remote` passed 9/9; scoped rustfmt/check passed for both touched leaf files.

## Pending
- Re-run the socket-dependent remote-server tests in an environment that permits loopback binds.

## Decisions
- Host actor remains field-absent and forced to `PlanApprovalActor::User` through the shared approval seam.
- Repository/read errors after claim must propagate, leaving `Starting` for stale settlement.
- No AppHandle, ExecutionState, or ChatService enters the request command or dispatcher signature.
- The existing `spawn_remote_resume_dispatchers` loop owns this intent as an additive third family; this avoids a new background-loop architecture and keeps a single stale sweep cadence.
- User explicitly prohibited `cargo clean`; no cleanup command was run despite the repository's general post-test cleanup rule.
- Dispatcher result serialization is terminal-state best effort after the host effect: failure to serialize is logged and persisted as JSON `null`, never propagated or converted into a host-action failure.
- Fix round 2 preserved the user override prohibiting `cargo clean`; no cleanup command was run after the requested Rust tests.
