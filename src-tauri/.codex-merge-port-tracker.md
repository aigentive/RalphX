# Post-#993 merge port tracker

## Scope

- Preserve extracted command bodies under `src/application` as the single definitions.
- Port EventSink emission, runtime transition builders, and explicit execution-state chat builders.
- Resolve compile/clippy/layering failures only under `src-tauri/**`.

## Done

- Read root/backend instructions and Rust test execution rules.
- Collapsed command-layer lifecycle, mutation scheduler/transition, recovery, and execution-state duplicates.
- Preserved extracted recovery prompt-tracker wiring.
- Ported EventSink emission in extracted resume/restart paths.
- Ported runtime transition builders and explicit execution-state chat builders.
- Threaded execution state through ideation apply/finalize variants and callers.
- Ported plan-approval notification dependency bundles in application and HTTP paths.
- Restored remote facade host-handle injection outside `AppState` and updated runtime registrations.
- `cargo check --tests --features test-utils` passes with zero warnings.
- Both requested clippy lanes pass with `-D warnings`.
- Layering ratchet passes; runtime paths no longer cross from startup background into commands.

## Pending

- Mark the resolved ideation command in Git's index (blocked by sandbox-denied index lock).

## Decisions

- Application-layer extractions remain authoritative; command-layer copies are removed.
- Application-owned execution/active-project state remains authoritative; the command module re-exports it.
- Event names, payloads, ordering, and best-effort error behavior remain unchanged.
- Remote Wry-only command injection uses a dedicated managed wrapper because `AppState` no longer stores a handle.
- The existing startup-background layering baseline fingerprint remains as disabled compatibility code because its baseline file is outside the authorized `src-tauri/**` scope.
