# Wave E1b tracker

## Scope
- Frontend-only repair of nine stale/guard test failures; preserve uncommitted Wave E1 work.

## Investigation
- Reproduced all nine failures: four stale capability/envelope assertions, one stale queue mock, two stale environment-switch setup assumptions, and two guard failures exposing dead rows/indirections plus one wrong-op gate.

## Decisions
- Capability availability follows the generated remote manifest; recovery assertions use the real `commandError` envelope.
- Removed six unused workspace-read affordance rows and two superseded execution rows; documented four legitimate indirections.
- Fixed real defects: Column Resume All now gates `groupResume`; ProposalWidget recognizes archive and renders “Archived”.

## Validation
- Focused: 6 files, 146 tests passed.
- Requested Vitest slice passed (exit 0) through a transient config-loader workaround for the read-only dependency symlink.
- Typecheck passed, including all pretypecheck drift/mirror/census gates and `tsc --noEmit`.
- Standalone transport-drift passed: 615 command names, 0 dynamic, 0 seam bypasses, 0 unclassified.
