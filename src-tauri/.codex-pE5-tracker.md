# Wave E5 tracker

## Scope

- Frontend swallow sweep only; duplicated task/agent detail files stay duplicated and receive identical fixes.
- Preserve successful-empty results while making failed/unavailable reads explicit.
- Fail reset-and-reaccept closed before reopen/apply when the plan base cannot be resolved.

## Progress

- [x] Read required project and remote-facade instructions.
- [x] Located all 12 requested sites and their existing tests.
- [x] Add behavioral regressions using real `commandError` envelope shapes.
- [x] Implement the two priority fixes.
- [x] Implement remaining read and mutation fixes in both duplicate trees.
- [x] Run requested full gates (Vitest attempted exactly and blocked before collection by read-only shared `node_modules/.vite-temp`; typecheck and drift passed).
- [x] Run scoped ESLint and record further swallowed-read instances without expanding scope.

## Decisions and findings

- `useResetAndReaccept` currently converts `getByPlan` rejection to `null`, then performs destructive reopen before applying without the prior base override.
- `useMessageAttachments` currently returns a partial/empty map after per-message failures; its result needs availability alongside the map so renderers can distinguish uncaptured host attachments from known-empty.
- Plan review history and task-detail context have identical duplicate implementations under `tasks/` and `agents/task-details/`; both copies will remain mechanically aligned, with dedupe noted as follow-up debt.
- The two ChangeReviewSection copies retain pre-existing presentation differences, but the history availability logic and tests are identical. The TaskDetailContext copies remain byte-identical; deduplication stays out of scope.
- Scoped ESLint: 0 errors; four pre-existing `react-hooks/exhaustive-deps` warnings in `App.tsx` at 693, 842, 870, and 928.

## Further instances outside requested scope

- No additional failed-read-to-empty/null UI claims were confirmed in the bounded catch scan. Remaining `catch(() => undefined/null)` hits are recovery-callback guards, event refresh best-effort work, preload/teardown side effects, or explicitly excluded delivered-payload/local-storage parsing; they were not expanded into this slice.
