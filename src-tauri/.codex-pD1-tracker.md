# Wave D slice 1 tracker

## Scope
- Piece 1: retain live host app version/platform in the non-persisted environment store and surface them in Connections UI.
- Piece 2: measure the ticket-work start spawn chain; add its process refusal; audit/register exactly five credential-free ticketing reads and gate their UI surfaces.
- Piece 3: rename the archive-only proposal command end to end; record later dead-command census candidates.

## Binding constraints
- Remote availability is inferred from manifest absence.
- Spawn-distinct paths remain distinct functions.
- Remote tests assert the real `input` envelope and per-call command/payload; optional schema args are ignored.
- No SQLite persistence for live host descriptor metadata.
- No startup background edits; loop hashes must remain unchanged.

## Findings / decisions
- Baseline clean at `2b71f1339`.
- Detector C passed before and after the explicit `start_ralphx_work_from_ticket` process refusal; the audited body reaches `AgentConversationStartService::start` and its launch chain.
- Exactly five ticketing commands were narrowed to capability-free `Read` rows and registered; the other fifteen remain unchanged.
- Provider summaries expose no `token_secret_ref`; the other four reads return repository/time data only and make no provider call.
- Host app version/platform live only in the in-memory environment store and refresh on every successful descriptor fetch; absent metadata renders nothing.
- `delete_task_proposal` was renamed end to end to `archive_task_proposal` and registered at `ui:agent` under the ideation module default.
- Later census candidate only (do not act): `delete_chat_message` and `delete_session_messages` have zero frontend callers.

## Validation
- PASS: layering, Cargo check, proposal filter, detector C, manifest regen, mirror update, census regen/check, drift scan, frontend typecheck, scoped rustfmt check.
- PARTIAL: ticketing filter ran 179 tests; 177 passed and 2 failed because sandbox permissions blocked worktree creation under `/Users/reefagent/ralphx-worktrees/...` (outside this workspace). No ticketing assertion failed.
- No Vitest run, Cargo clean, commit, push, install, or startup-background edit.
