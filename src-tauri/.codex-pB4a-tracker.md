# Wave B4a tracker

## Scope
- Add direct spawn-free `update_automation_config` Tauri/remote twin with optimistic version guard.
- Route remote setup edits through the twin while preserving the local HTTP path.
- Register/classify/regenerate remote facade artifacts and add focused backend/frontend tests.

## Constraints
- TDD first; production entry-path assertions.
- No startup background changes; no Vitest, cargo clean, commit, push, or install.
- Scoped Rust formatting and requested gate tails only.

## Status
- [x] Read required project/backend/remote-facade instructions.
- [x] Inspect owning patterns and existing tests.
- [x] Add focused command-entry and frontend API tests (Vitest intentionally not run).
- [x] Implement command and frontend transport branch.
- [x] Wire registry/ledger/generate_handler and regenerate artifacts.
- [x] Run requested gates and record raw tails.

## Findings
- HTTP applies settings first and config second, then returns `AutomationResponse`.
- Direct twin uses automation id authority, not setup conversation binding.
- The remote API takes the already-loaded automation so `automationId` and `expectedUpdatedAt`
  come from the displayed row; local HTTP dispatch/body remain unchanged.
- Generated manifest, agent-control mirror, and coverage census updated; startup loop hash untouched.
- `cargo check --tests --features test-utils` passed with zero warnings.
- `cargo test --lib --features test-utils automation` passed: 581 passed, 0 failed.
- Layering, remote drift, frontend typecheck, and scoped rustfmt checks passed.
- Per request, Vitest and `cargo clean` were not run.
