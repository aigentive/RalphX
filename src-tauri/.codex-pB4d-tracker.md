# Wave B4d tracker

## Scope
- Wire paired-client automation run-now, retry-judge, and create-draft through remote intent polling.
- Flip only the corresponding agent-gate rows; automation delete rows remain absent.
- Add focused frontend API and gate tests without running Vitest or Playwright.

## Constraints
- Touch only `frontend/src/**` and this tracker.
- Preserve local invoke branches byte-for-byte.
- Never edit generated capability files by hand.
- Run only the requested typecheck and node gates.

## Findings
- `src-tauri/.codex-pB4d-tracker.md` was absent at task start.
- Existing paired-client precedent is the queue send-now intent poller in `frontend/src/api/chat.ts`.
- The remote draft twin requires explicit defaults matching the local creation path: `Automation setup`, `reviewed`, `project_default`, and `isolated`.
- Run actions preflight through `get_automation` to pin the highest visible `runIndex`; stale-run and benign retry settlements re-read detail.

## Progress
- [x] Read required project and scoped rules.
- [x] Write focused tests first.
- [x] Implement gate and API polling changes.
- [x] Run requested gates and record raw output.

## Validation
- `cd frontend && bun run typecheck` — passed (prechecks plus `tsc --noEmit`).
- `node scripts/check-remote-transport-drift.mjs .` — passed; 602 invoke names, 0 dynamic, 0 seam bypasses, 0 unclassified.
- `node scripts/generate-remote-coverage-census.mjs --check .` — passed; 0 unclassified across 14 batches.
- Vitest and Playwright were not run, per task constraint.
