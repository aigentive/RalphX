# Wave E6 tracker

## Status
- Complete: metadata twin, manifest/gate wiring, frontend transport routing, tests, and regeneration are implemented.

## Constraints
- Metadata only; never expose the host `file_path` or attachment bytes.
- Remote twin must be commands-resident, spawn-free, `RiskClass::Read`, `caps: []`, and absence-gated.
- Preserve the local attachment path byte-for-byte.
- Do not touch `remote_server/attachments.rs` or `startup_background.rs`; do not run `cargo clean`.

## Findings
- Live `probe_b13_module_batch_audit` measurement (temporarily extended, then restored) reported `list_message_attachments a=false b=false c=false d=false visited=4 class=Denied caps=[WritesArbitraryPath] registered=false`.
- Therefore the detectors agree the command is a pure read; its denied/path-write classification is inherited policy, not earned authority. Implementation may proceed.
- The remote response type has no `file_path`; its real serialized envelope was asserted to contain id, filename, MIME, size, conversation/message identity, and timestamp while containing neither the field nor host-path bytes.
- Attachment query identity now includes local/remote transport plus gate availability so a cached local path-bearing result cannot bleed into a paired environment after a switch.
- `startup_background.rs` and loop hashes were untouched; manifest regeneration succeeded without loop-hash drift.

## Validation
- PASS `python3 scripts/check-layering.py`.
- PASS `cargo check --tests --features test-utils` (zero warnings; rerun after final Rust test placement).
- PASS `RUST_MIN_STACK=8388608 cargo test --lib --features test-utils attachment` (113 passed).
- PASS `RUST_MIN_STACK=8388608 cargo test --lib --features test-utils remote_attachment` (2 passed).
- PASS manifest regeneration, capability mirror update, census regeneration, and drift check.
- PASS focused frontend attachment suite (5 passed) and `bun run typecheck`.
- FULL frontend suite: 787 files / 12,820 tests passed, then Vitest failed the gate for one unrelated unhandled `target.hasPointerCapture is not a function` exception attributed to `ProjectCreationWizard.test.tsx`; no E6 test failed.
- PASS surgical `rustfmt --edition 2021 --check` and `git diff --check`.
- Per explicit user constraint, did not run `cargo clean`.
