# Wave C3 tracker

## Scope
- Add durable MCP catalog snapshots and a spawn-free remote read twin.
- Wire backend/remote registries, capability ledger, generated manifest/census, and MCP frontend remote routing/absence UI.
- Do not touch the parallel owner files named in the request.

## Findings
- Required tracker was absent at turn start; created here for context preservation.
- `get_mcp_catalog` and `refresh_mcp_catalog` share `build_catalog`; the snapshot write belongs at its successful tail.
- Remote-facade rules require a separate twin function whose closure names neither provider eligibility nor catalog discovery.
- User explicitly forbids `cargo clean` and Vitest for this slice; those override repository defaults.
- Parallel work claimed migration `20260805190000` while this slice was in progress; the MCP snapshot uses the next additive stamp, `20260805191000`.
- Snapshot persistence is a dedicated root repository because the stored value is a serialized command projection, not provider policy domain state.

## Plan
- [x] Add migration and snapshot repository trait/SQLite/memory implementations with focused tests.
- [x] Add AppState wiring and successful local write-through without changing returned catalog.
- [x] Add spawn-free remote twin and authority test; registry/ledger rows and generated artifacts complete.
- [x] Route frontend reads remotely, gate refresh, render snapshot absence, and add unrun Vitest coverage.
- [ ] Run requested gates in order, preserve raw tails, and review scoped diff.

## Validation
- Migration validator: PASS (263 migrations; latest `20260805191000`).
- `cargo check --tests --features test-utils`: PASS, zero warnings on final run.
- `cargo test ... mcp`: C3 tests pass; overall filter blocked by three unrelated loopback-bind capability tests (`Operation not permitted`), 264 passed / 3 failed / 1 ignored.
- `cargo test ... remote_mcp`: PASS, 3/3.
- Manifest regeneration: PASS, 1/1; no startup loop-hash complaint.
- Mirror update: 274 facade ops; census regeneration: 0 unclassified.
- Remote transport drift: PASS (609 names; 0 dynamic/bypasses/unclassified).
- Frontend `bun run typecheck`: PASS. Vitest intentionally not run.
- Layering: blocked by parallel slice's `src/http_server/handlers/projects.rs -> crate::commands::project_commands::cached_repository_capability` import; no C3 file named.
- Scoped Rustfmt: all C3-owned leaves pass; shared `app_state.rs` check reports formatting only around the parallel project-capability additions, left untouched.
- No `cargo clean` run, per explicit user constraint.

## Coordination
- Parallel agent owns `commands/project_commands.rs`, `commands/remote_workspace_commands.rs`, project repository capability migration, `frontend/src/types/project.ts`, and `RepositorySettingsSection.tsx`; ignore their diffs.
