[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "link-arg=-fuse-ld=lld"]

# macOS uses the platform linker. Benchmark any macOS rustflags separately
# before landing them because rustflags invalidate Cargo caches.
